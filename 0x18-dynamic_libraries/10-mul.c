#include "main.h"
/**
 *mul- program that multiplies tow integers
 *Return: result success
 *@a:itegers to be mult
 *@b:INTEGERS TO BE mult by
 */
int mul(int a, int b)
{
int result = a * b;
return (result);
}
