#include "main.h"
/**
 *sub- program that subs tow integers
 *Return: result success
 *@a:itegers to be subedd
 *@b:INTEGERS TO BE subed from
 */
int sub(int a, int b)
{
int result = a - b;
return (result);
}
