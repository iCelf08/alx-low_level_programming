#include "main.h"
/**
 *div- program that devises tow integers
 *Return: result success
 *@a:itegers to be divised
 *@b:INTEGERS TO BE devised by
 */
int div(int a, int b)
{
int result = a / b;
return (result);
}
