#include "main.h"
/**
 *mod- program that modulize tow integers
 *Return: result success
 *@a:itegers to module
 *@b:INTEGERS TO BE moduled by
 */
int mod(int a, int b)
{
int result = a % b;
return (result);
}
