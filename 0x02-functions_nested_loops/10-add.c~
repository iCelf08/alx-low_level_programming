#include "main.h"
/**
 *add- program that adds tow integers
 *Return: result success
 *@a:itegers to be added 
 *@b: INTEGERS TO BE ADDed
 */
int add(int a, int b)
{
int result = a + b;
return (result);
}
